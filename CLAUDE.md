## Local AI Notes
- ALWAYS use English for code comments, documentation, commit messages, and variable names.
- NEVER add new fields to `config/Tenants.json` or `config/Credentials.json` without asking for user approval first. These are shared tenant configuration files and any schema changes require explicit authorization.
